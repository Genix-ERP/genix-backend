package handler

// Expense category handlers already exist in expense.go (ListExpenseCategories,
// CreateExpenseCategory, DeleteExpenseCategory). This file intentionally left
// empty to avoid duplicate declarations.
